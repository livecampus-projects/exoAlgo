// Demander à l’utilisateur deux nombres A (colonnes) et B (lignes), renvoyer le résultat (1,2,3) selon ce tableau :
// Prévoir les cas non valides (blanc / hors tableau)

Algorithme calculer_resultat
    Variable A, B, return: entier
    Début
        // Étape 1 : Demander à l'utilisateur de saisir les nombres A et B
        afficher("Entrez la valeur de A (colonnes) : ")
        lire(A)
        afficher("Entrez la valeur de B (lignes) : ")
        lire(B)

        // Vérifier la première ligne
        Si (A >= 98 ET A <= 189) ET (B >= 10 ET B <= 20)
            Si (A >= 98 ET A <= 172)
                return <- 1
            Sinon Si (A >= 173 ET A <= 189)
                return <- 2
            Fin Si
        Fin Si

        afficher("Le résultat est " & return)
    Fin
